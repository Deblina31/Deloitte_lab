var principal=300000;
var time=3;
var rateOfInterest=0.12;

var simpleInterest=(principal*rateOfInterest*time)/100;
console.log(`Simple Interest for ${principal} at ${rateOfInterest}
    for the duration ${time} is ${simpleInterest}`);
